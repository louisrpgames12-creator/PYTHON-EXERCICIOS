palavra_secreta = input('me informe uma palavra: ')
tamanho_palavra_secreta = len(palavra_secreta)
tamanho_letra = 0
palavra = ''
repeticao = 1

while tamanho_letra != 1 :
    letra = input('Me informe uma letra: ')
    tamanho_letra = len(letra)
for letra_palavra_secreta in palavra_secreta :
    if letra_palavra_secreta == letra :
        print(letra, end='')
        palavra += letra_palavra_secreta
    else :
        print('*', end = '')
        palavra += '*'
print('')
print(palavra)

while palavra != palavra_secreta:
    i = 0
    palavra2 = ''
    tamanho_letra = 0
    while tamanho_letra != 1 :
        letra = input('Me informe uma letra: ')
        tamanho_letra = len(letra)
    for letra_palavra_secreta in palavra_secreta :
        if letra_palavra_secreta == letra :
            palavra2 += letra_palavra_secreta
            print(palavra2[i], end='')
        elif letra_palavra_secreta != letra :
            palavra2 += palavra[i]
            print(palavra2[i], end = '')
        i += 1
    print('')
    repeticao += 1
    palavra = palavra2

print(f'a palavra é {palavra}\n Repetiu {repeticao}')
